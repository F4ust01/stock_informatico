export const PORT: string = process.env.PORT || "3000"; // Valor por defecto
export const URI: string = process.env.URI || "mongodb://localhost:27017/inventoryDB"; // Si no está definido, asignamos una cadena vacía
